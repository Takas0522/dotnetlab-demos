{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run/Debug logic app logic-dotnetlab-202509",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureLogicAppsStandard.pickProcess}"
        }
    ]
}